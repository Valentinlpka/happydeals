import 'package:flutter/material.dart';

class PaymentCancel extends StatelessWidget {
  const PaymentCancel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Vous avez annulé votre achat.'),
    );
  }
}
